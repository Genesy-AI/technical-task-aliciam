# Tech Roadmap Improvements

## Planned improvements

- Add per-lead email enrichment progress reporting in UI
- Refactor  CSV parser tests to improve readability through object row checks and parameterized tests
- Include `emailVerified` in the lead object created as part of the optimistic update in `LeadsList`
- Add a limit to the number of email verification processes that can run at once